import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "MSBTE Master Scraper — Automate Results in Seconds",
  description:
    "Automate MSBTE seat processing. Zero effort, 100% accuracy, running completely local on Windows.",
  keywords: ["MSBTE", "result scraper", "automation", "Windows tool"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased bg-white text-slate-900">
        {children}
      </body>
    </html>
  );
}
